<h2><%= t(:show_tag_headline) %> »<%=h @tag %>«</h2>

<%= 
    render(
      :partial => 'custom/partials/article',
      :collection => @pages,
      :as => :page
    )
  %>
  
<%= will_paginate @pages %>
